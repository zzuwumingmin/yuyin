day1 19.12.30（按照时间顺序完成的任务）
第一，安装python，配置电脑环境，配置pycharm以及windows自带的powershell的相关pip install操作。
第二，学习基础语法 print（‘...’）或者（“...”+“...”）；+可以实现字符串的拼接（“.......\n换行”*数字）语句的多次输出；print（语句+数字）格式错误，类型不同。
第三，Ctrl+/代码注释；input内置函数可以输出一段并读取用户接下来输入的内容 temt = input('')；if...：（冒号）else：（冒号）判断
第四，下载安装anaconda。
第五，变量名不能以数字开头，字母大小写表示的值不同，尽量取专业一些的名字。单引号或者双引号里的表示是字符串。引号内部出现引号,需要加(\)，或者里面是单引号的话，外面就使用双引号。表示路径时，C：\now路径中的\可以在之前再加一个\才可以显示完全。或者直接定义时加r，如str=r'...'.
